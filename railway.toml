[build]
builder = "NIXPACKS"

[deploy]
# startCommand is intentionally omitted here — set per-service in the Railway UI:
#   Tracker: node index.js
#   Greg (wizard): node wizard.js
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
